import React from 'react';

const Mobiles = () => {
  return (
    <div style={{ padding: '20px' }}>
      <h1>Mobiles Page</h1>
      <p>All mobile-related products will appear here.</p>
    </div>
  );
};

export default Mobiles;
//                   href={`/${menu.title.toLowerCase().replace(/ & | /g, "-")}/${item.toLowerCase()}`}